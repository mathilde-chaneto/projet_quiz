{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}

{% block body %}
    {{ parent() }}
    <main>
        <section id="quiz-edit-container">
            <h1>
                Réponses - Suppresion		     
            </h1>
            


    <h2>Formulaire de Suppresion </h2>
        
        {{form_start(formAnswer)}}
                   
        {{form_row(formAnswer.nameAnswer)}}
        
        {{form_row(formAnswer.is_correct)}}
    
        {{form_row(formAnswer.questions)}}	
    
    
      
        
        <div class="control">
            <button id="submit">Envoyer</button>
        <button id="back"><a href="{{ path('dev-quiz_answers') }}">Retour</a></button>
        </div>
    
        {{form_end(formAnswer)}}


  
    </section>
</main>
{{ include('main/footer.html.twig') }}
{% endblock %}