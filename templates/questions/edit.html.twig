{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}

{% block body %}
    {{ parent() }}
    <main>
        <section id="quiz-edit-container">
            <h1>
                Question - Edition		     
            </h1>
            


    <h2>Formulaire d'édition de question </h2>

        {{form_start(form)}}
           
        {{form_row(form.title)}}
        
        {{form_row(form.infoplus)}}
    
        {{form_row(form.quiz)}}	
    
    
      
        
        <div class="control">
            <button id="submit">Envoyer</button>
        <button id="back"><a href="{{ path('dev-quiz_questions') }}">Retour</a></button>
        </div>
    
        {{form_end(form)}}


  
    </section>
</main>
{{ include('main/footer.html.twig') }}
{% endblock %}